export enum ParkElements {
    D = "decoration",
    R = "road",
    P = "park",
    EMPTY = 'empty'
}
export enum Statuses{
    SUCCESS = 'fulfilled',
    ERROR = 'rejected',
    LOADING = 'loading'
} 